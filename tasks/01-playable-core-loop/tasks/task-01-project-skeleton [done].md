# Task: Create Project Skeleton [done]

## Goal

Create the base React, TypeScript, Vite, and Canvas project structure for Bee Up!.

## Scope

- Add `package.json`, Vite config, TypeScript config, ESLint config.
- Add `src/client/App.tsx`, `src/client/main.tsx`, and global styles.
- Add game module folders under `src/client/game`.

## Acceptance Criteria

- `npm install` works.
- `npm run typecheck` works.
- `npm run lint` works.
- `npm run build` works.

## Status

Done in current MVP.
